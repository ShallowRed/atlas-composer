/**
 * Invert Utilities
 *
 * Pure functions for inverse projection with bounds validation.
 * Used by composite projections to determine which territory
 * a screen point belongs to.
 */

export { invertWithBoundsValidation } from './validator'
