/**
 * Region Configurations
 * Centralized configuration for all available geographic regions
 *
 * @deprecated This file is deprecated. Use @/config/regions instead.
 * Re-exporting from new location for backward compatibility.
 */

export {
  DEFAULT_REGION,
  getAvailableRegions,
  getRegionConfig,
  REGION_CONFIGS,
} from '@/config/regions'
