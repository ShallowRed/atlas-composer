# Preset files will be created by exporting from the running application
# 
# To create a preset:
# 1. Run the application with `pnpm dev`
# 2. Select the atlas (e.g., France)
# 3. Switch to composite-custom view mode
# 4. Adjust territory positions, projections, and scales as desired
# 5. Use the Export Configuration feature
# 6. Save the exported JSON to this directory
# 7. Name the file following the pattern: {atlas-id}-default.json
#
# Required presets:
# - france-default.json
# - portugal-default.json
# - spain-default.json
