#!/usr/bin/env tsx
/**
 * DEPRECATED: Conversion script no longer needed
 *
 * All preset files have been converted to use pixelClipExtent format.
 * This script is kept for reference only.
 */

console.log('✓ All presets already use pixelClipExtent format - no conversion needed')
process.exit(0)
